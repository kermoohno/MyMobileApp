import { SafeAreaView } from "react-native";
import Splash from "./src/screens/auth/Splash";

const App = () => {
  return(
    <SafeAreaView>
      <SignUp />
    </SafeAreaView>
  )
}

export default App